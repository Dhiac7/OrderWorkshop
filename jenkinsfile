pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', 
                    url: 'https://github.com/Dhiac7/OrderWorkshop.git',
                    credentialsId: 'github-token'
            }
        }

        
    }
}
